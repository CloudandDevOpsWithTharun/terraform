# efs-storageclass.yaml

apiVersion: storage.k8s.io/v1
kind: StorageClass

metadata:
  name: efs-sc

provisioner: efs.csi.aws.com

parameters:
  provisioningMode: efs-ap
  fileSystemId: fs-028d52b8167d6e124
  directoryPerms: "777"

mountOptions:
  - tls

reclaimPolicy: Retain

volumeBindingMode: Immediate
